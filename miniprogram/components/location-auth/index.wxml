<!-- 位置授权组件 -->
<view class="location-auth" wx:if="{{show}}" catchtap="preventTap">
  <view class="overlay fade-in"></view>
  <view class="modal-container slide-up">
    <!-- 图标 -->
    <view class="icon-wrap">
      <view class="icon-circle">
        <text class="icon">📍</text>
      </view>
    </view>
    
    <view class="modal-title">获取位置信息</view>
    
    <view class="modal-content">
      <text wx:if="{{authStatus !== 'denied'}}">
        为了为您推荐附近的课程和小区，需要获取您的地理位置信息
      </text>
      <text wx:else>
        您已拒绝位置授权，请在设置中开启位置权限，以便为您推荐附近的课程
      </text>
    </view>
    
    <view class="modal-actions">
      <button class="btn btn-secondary" bindtap="onSkip">暂不授权</button>
      <button 
        class="btn btn-primary" 
        wx:if="{{authStatus !== 'denied'}}"
        bindtap="onRequestAuth"
      >
        允许授权
      </button>
      <button 
        class="btn btn-primary" 
        wx:else
        bindtap="onOpenSetting"
      >
        去设置
      </button>
    </view>
  </view>
</view>
