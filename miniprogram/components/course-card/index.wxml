<!-- 课程卡片组件 - 网格布局版本 -->
<view class="course-card" bindtap="onTap">
  <!-- 课程图片 -->
  <view class="card-image">
    <image 
      class="image" 
      src="{{imageUrl}}" 
      mode="aspectFill"
      binderror="onImageError"
    />
  </view>
  
  <!-- 课程信息 -->
  <view class="card-content">
    <!-- 课程名称 -->
    <view class="course-name">{{course.name || '体能课程'}}</view>
    
    <!-- 小区信息 -->
    <view class="info-row">
      <text class="icon">📍</text>
      <text class="info-text truncate">{{course.community_name || '未知小区'}}</text>
    </view>
    
    <!-- 时间信息 -->
    <view class="info-row">
      <text class="icon">🕐</text>
      <text class="info-text">{{course.schedule || '待定'}}</text>
    </view>
    
    <!-- 班级号 -->
    <view class="info-row" wx:if="{{course.class_no}}">
      <text class="icon">📋</text>
      <text class="info-text">班级号：{{course.class_no}}</text>
    </view>
    
    <!-- 成班信息 -->
    <view class="enroll-row">
      <text class="enroll-label">满{{course.min_students || 4}}人成班</text>
    </view>
    
    <!-- 底部：报名进度和按钮 -->
    <view class="card-footer">
      <view class="progress-section">
        <view class="avatar-group">
          <view class="avatar-item" wx:for="{{avatarList}}" wx:key="index">
            <image class="avatar-img" src="{{item}}" mode="aspectFill" />
          </view>
        </view>
        <text class="enroll-count">{{course.enrolled_count || 0}}/{{course.max_students || 10}}人</text>
      </view>
      
      <button class="join-btn" catchtap="onJoin">
        去拼班
      </button>
    </view>
  </view>
</view>
