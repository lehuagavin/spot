<!-- 选择学员页面 -->
<view class="page-container">
  <view class="tip-bar">请优先选择学员</view>
  
  <view class="loading-state" wx:if="{{loading}}">
    <view class="loading-spinner"></view>
  </view>
  
  <view class="student-list" wx:elif="{{students.length > 0}}">
    <view 
      class="student-item {{item.selected ? 'selected' : ''}}"
      wx:for="{{students}}"
      wx:key="id"
    >
      <!-- 头像 -->
      <view class="student-avatar" bindtap="onToggleSelect" data-id="{{item.id}}">
        <image wx:if="{{item.photo}}" src="{{item.photo}}" mode="aspectFill" class="avatar-img"/>
        <text wx:else class="avatar-placeholder">{{item.gender === 1 ? '👦' : '👧'}}</text>
      </view>
      
      <!-- 学员信息 -->
      <view class="student-info" bindtap="onToggleSelect" data-id="{{item.id}}">
        <view class="info-header">
          <text class="student-name">{{item.name}}</text>
          <view class="member-tag {{item.member_type === 'vip' ? 'vip' : ''}}">
            {{item.member_type === 'vip' ? 'VIP会员' : '普通会员'}}
          </view>
        </view>
        <view class="student-birthday">出生日期：{{item.birthday}}</view>
      </view>
      
      <!-- 删除按钮 -->
      <view class="delete-btn" catchtap="onDeleteStudent" data-id="{{item.id}}" data-name="{{item.name}}">
        <text class="delete-text">删除</text>
      </view>
      
      <!-- 选择框 -->
      <view class="check-box {{item.selected ? 'checked' : ''}}" bindtap="onToggleSelect" data-id="{{item.id}}">
        <text wx:if="{{item.selected}}">✓</text>
      </view>
    </view>
  </view>
  
  <view class="empty-state" wx:else>
    <text class="empty-icon">👶</text>
    <text class="empty-text">暂无学员</text>
  </view>
  
  <view class="bottom-placeholder"></view>
</view>

<view class="bottom-bar">
  <button class="add-btn" bindtap="onAddStudent">新增学员</button>
  <button class="confirm-btn" bindtap="onConfirm">确认选择</button>
</view>
